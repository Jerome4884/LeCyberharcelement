// Script for display youtube video on Firefox, because firefox have a security which block youtube video
// Fonction appelée lorsque l'API JavaScript YouTube est chargée
function onYouTubeIframeAPIReady() {
    // Créer un lecteur vidéo YouTube
    let player = new YT.Player('player', {
      videoId: 'https://www.youtube.com/embed/zUW3JyLtYsQ',
      playerVars: {
        // Ajoutez ici les paramètres du lecteur vidéo (facultatif)
      },
      events: {
        // Ajoutez ici les gestionnaires d'événements du lecteur vidéo (facultatif)
      }
    });
  }
 
  // Appeler la fonction onYouTubeIframeAPIReady() lorsque la page est chargée
  document.addEventListener('DOMContentLoaded', function() {
    onYouTubeIframeAPIReady();
  });
// Fonction appelée lorsque l'API JavaScript YouTube est chargée
function onYouTubeIframeAPIReady() {
  // Créer un lecteur vidéo YouTube
  let player3018 = new YT.Player('player3018', {
    videoId: 'https://www.youtube.com/watch?v=1e3ngFNgcAQ',
    playerVar: {
      // Ajoutez ici les paramètres du lecteur vidéo (facultatif)
    },
    events: {
      // Ajoutez ici les gestionnaires d'événements du lecteur vidéo (facultatif)
    }
  });
}

// Appeler la fonction onYouTubeIframeAPIReady() lorsque la page est chargée
document.addEventListener('DOMContentLoaded', function() {
  onYouTubeIframeAPIReady();
});
function onYouTubeIframeAPIReady() {
  // Créer un lecteur vidéo YouTube
  let p3018 = new YT.Player('p3018', {
    videoId: 'https://www.youtube.com/watch?v=CLLzd0Y6o-w',
    playerVars: {
      // Ajoutez ici les paramètres du lecteur vidéo (facultatif)
    },
    events: {
      // Ajoutez ici les gestionnaires d'événements du lecteur vidéo (facultatif)
    }
  });
}

// Appeler la fonction onYouTubeIframeAPIReady() lorsque la page est chargée
document.addEventListener('DOMContentLoaded', function() {
  onYouTubeIframeAPIReady();
});
